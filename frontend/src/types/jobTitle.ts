// A job title / designation a staff member holds (employee master-data). Managed
// from Users & Roles; the user form picks from this list or creates one inline.
export interface JobTitle {
  id: string;
  name: string;
  createdAt: string;
}
