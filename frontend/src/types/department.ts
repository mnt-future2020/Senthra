// A department a staff member can belong to (employee master-data). Managed from
// Users & Roles; the user form picks from this list or creates one inline.
export interface Department {
  id: string;
  name: string;
  createdAt: string;
}
